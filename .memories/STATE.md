# STATE
> Снимок состояния. Last updated: 2026-09-07.

## Сделано `[DONE]`

- Модель проекта версии 2: дорожки (`content`/`overlay`/`audio`) и элементы со свободным размещением,
  разбор и перенос старого формата (`src/shared/normalize.ts`).
- Математика кадра, раскладка дорожек, пресеты, ASS, сборка ffmpeg-графа: `src/shared`.
- Сервер: конфиг, поиск ffmpeg и NVENC, ingest за один проход, экспорт с прогрессом, хранилища, API, WS,
  диалог файлов (PowerShell), просмотр папок, окно Edge/Chrome `--app`.
- Пики звука на C через `bun:ffi cc()` (TinyCC внутри Bun), запасной путь на TS. На Windows x64 работает.
- Экспорт: лента базы (concat, чёрные зазоры, xfade на наездах), наложения через overlay с поворотом
  и прозрачностью, звук всех дорожек через amix, текст и субтитры одним ASS.
- Картинки и звуковые файлы: подготовка (`still.jpg`, `proxy.m4a`), свои правила длительности.
- Интерфейс: медиатека с перетаскиванием на дорожки, превью с наложениями, транспорт, многодорожечный
  таймлайн (перенос, растягивание за края, прилипание, перенос между дорожками), инспектор
  (элемент, цвет, звук, субтитры, экспорт), тянущиеся границы панелей, отмена/повтор, горячие клавиши.
- Тесты: 58 (`bun test`), типы чистые в обоих tsconfig. Три независимых ревью пройдены, все существенные замечания закрыты (журнал 07.09.2026).
- Exe собран и проверен: `dist/ki-video.exe` 94 МБ, страница, ffmpeg, ingest и C-пики работают. Dockerfile написан, образ не собирался.

## Измерено (RTX 3060, ffmpeg 8.1, машина частично занята другим процессом)

| Операция | Результат |
|---|---|
| Подготовка клипа 30 с HEVC 1728×1080@60 (прокси + миниатюры + PCM) | 4,5 с |
| Экспорт проекта 60 с, 1080×1920@60, H.264 NVENC, все фильтры + ASS | 33,6 с (1,73×) |
| Экспорт проекта 17 с после правок (граф в файле) | 9,7 с |
| Пики звука 30 с: C против TS | 0,57 мс против 1,89 мс |
| Сборка страницы Bun (HTML-импорт) | 46 мс |
| `bun run build` (exe 94 МБ) | ~10 с |

## Проверено на живом ffmpeg (07.09.2026, n9.0.1, libx264)

Проект из наезда с `slideleft`, зазора, картинки в кадре, повёрнутого полупрозрачного наложения с фейдами,
текста, субтитра и музыки: экспорт 8,5 с за 3,5 с, кадры на 1 / 2,7 / 6 / 7 с проверены глазами.

## Не сделано / отложено `[TODO]`

- Интерфейс после переработки на дорожки в браузере не гонялся.
- Цвет и звук по-прежнему на весь проект, не на элемент.
- Скорость элемента (setpts/atempo), поворот и зеркало базы.
- Предпросмотр звуковой цепочки (WebAudio: highpass и компрессор можно, шумодав нет).
- Кроссфейд в превью: на наезде видно верхний элемент, переход показывается только в экспорте.
- Гамма, сочность, резкость в превью (нужен WebGL или CSS-фильтры не покрывают).
- Распознавание речи для субтитров (кандидат: whisper.cpp через `bun:ffi`).
- Нативное окно вместо Edge `--app` (WebView2 через FFI или сторонний биндинг).
- Тесты сервера (range, progress parser, probe) есть только для чистых функций общего кода.
