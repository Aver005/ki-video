# STATE
> Снимок состояния. Last updated: 2026-09-07.

## Сделано `[DONE]`

- Модель, математика кадра, таймлайн, пресеты, ASS, сборка ffmpeg-графа: `src/shared`.
- Сервер: конфиг, поиск ffmpeg и NVENC, ingest за один проход, экспорт с прогрессом, хранилища, API, WS,
  диалог файлов (PowerShell), просмотр папок, окно Edge/Chrome `--app`.
- Пики звука на C через `bun:ffi cc()` (TinyCC внутри Bun), запасной путь на TS. На Windows x64 работает.
- Интерфейс: медиатека, превью на canvas, транспорт, таймлайн, инспектор (кадр, цвет, звук, текст,
  субтитры, экспорт), отмена/повтор, горячие клавиши.
- Тесты: 46 (`bun test`), типы чистые в обоих tsconfig. Три независимых ревью пройдены, все существенные замечания закрыты (журнал 07.09.2026).
- Exe собран и проверен: `dist/ki-video.exe` 94 МБ, страница, ffmpeg, ingest и C-пики работают. Dockerfile написан, образ не собирался.

## Измерено (RTX 3060, ffmpeg 8.1, машина частично занята другим процессом)

| Операция | Результат |
|---|---|
| Подготовка клипа 30 с HEVC 1728×1080@60 (прокси + миниатюры + PCM) | 4,5 с |
| Экспорт проекта 60 с, 1080×1920@60, H.264 NVENC, все фильтры + ASS | 33,6 с (1,73×) |
| Экспорт проекта 17 с после правок (граф в файле) | 9,7 с |
| Пики звука 30 с: C против TS | 0,57 мс против 1,89 мс |
| Сборка страницы Bun (HTML-импорт) | 46 мс |
| `bun run build` (exe 94 МБ) | ~10 с |

## Не сделано / отложено `[TODO]`

- Переходы между клипами (xfade), несколько дорожек.
- Предпросмотр звуковой цепочки (WebAudio: highpass и компрессор можно, шумодав нет).
- Гамма, сочность, резкость в превью (нужен WebGL или CSS-фильтры не покрывают).
- Распознавание речи для субтитров (кандидат: whisper.cpp через `bun:ffi`).
- Перетаскивание клипов мышью по таймлайну (сейчас кнопки «раньше/позже»).
- Нативное окно вместо Edge `--app` (WebView2 через FFI или сторонний биндинг).
- Тесты сервера (range, progress parser, probe) есть только для чистых функций общего кода.
